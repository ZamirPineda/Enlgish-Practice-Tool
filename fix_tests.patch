--- src/components/DailyProgressWidget.test.tsx
+++ src/components/DailyProgressWidget.test.tsx
@@ -86,7 +86,7 @@
     expect(screen.getByText(/2 \/ 2/i)).toBeInTheDocument();

     const rewardButton = screen.getByRole("button", {
-      name: "Claim +40 XP",
+      name: /Claim \+40 XP/i,
     });
     expect(rewardButton).toBeEnabled();

@@ -118,7 +118,7 @@
     expect(screen.getByText(/3 \/ 7 active days/i)).toBeInTheDocument();

     const weeklyClaimButton = screen.getAllByRole("button", {
-      name: "Claim Reward",
+      name: /Claim Reward/i,
     })[0];

     act(() => {
